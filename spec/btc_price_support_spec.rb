require "spec_helper"

RSpec.describe BtcPriceSupport do
  it "has a version number" do
    expect(BtcPriceSupport::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
